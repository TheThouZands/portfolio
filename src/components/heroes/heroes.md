Heroes are partials that are to be used at the top of pages, are to be simplistic, and bold, at least user-facing

on JS and CSS may be heavy, since their purpose is to be impactful, communicates with header to override default
behavior with a certain transition, new text or others, sits below per page overrides
